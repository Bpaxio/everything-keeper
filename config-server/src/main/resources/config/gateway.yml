server:
  port: 8080

eureka:
  client:
    fetch-registry: true

zuul:
  sensitive-headers: ""
  prefix: "/api"
  strip-prefix: true
  routes:
    files:
      serviceId: files-keeper
      strip-prefix: true
    meta:
      serviceId: things-keeper
      strip-prefix: true
